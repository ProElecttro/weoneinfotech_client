import { useEffect } from "react";

const Profile = () => {
  const fetchProfile = async () => {
    try {
        const url = ""
    } catch (error) {
        
    }
  };

  useEffect(() => {
    fetchProfile()
  }, []);
  return <div></div>;
};

export default Profile;
